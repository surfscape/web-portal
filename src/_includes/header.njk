<header class="flex container">
  <span class="header--site--title">{{site.title}}</span>
  <nav class="header--nav flex">
    {% for menuNav in nav.menuNav %}
      <a class="container" href="{{menuNav.url}}">{{menuNav.title}}</a>
    {% endfor %}
  </nav>
</header>